@using Nodsoft.MoltenObsidian.Vault


@(_foundEntity switch 
{
	_ when CurrentPath is null or "" or "/" => Index(Vault),
	IVaultFolder folder => FoundFolder(folder),
	IVaultNote file => FoundFile(file),
	_ => NotFound(_currentPath)
})